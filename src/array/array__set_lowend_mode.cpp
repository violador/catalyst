//
//
//
inline void set_lowend_mode(std::string scratch_dir = DEFAULT_CURRENT_DIR)
{
/*
    if(scratch_dir == "" and setup_ready)
    {
        temp_filename = config -> dir_path_of(SCRATCH) + array_filename;
    }
    else
    {
        temp_filename = scratch_dir + array_filename;
    }
//
    global_log::file.write_debug_msg("array::set_lowend_mode(): Array ID = ", 
                                     array_id, 
                                     ", temp file = ", 
                                     temp_filename,
                                     ". Invoking array::book_data()");
//
    book_data();
    lowend_mode_on = true;
*/
};
