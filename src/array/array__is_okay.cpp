//
//
//
inline bool is_okay()
{
	switch(!(data1 == NULL && data2 == NULL
	                       && data3 == NULL
	                       && data4 == NULL) && (!constant))
	{
		case false: return false;
		case  true: return true;
	}
};
